import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('PiyeKabare', () => App);

AppRegistry.runApplication('PiyeKabare', {
    initialProps: {},
    rootTag: document.getElementById('root'),
});
